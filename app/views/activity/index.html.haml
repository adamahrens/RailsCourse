%h1 Activites
- @activities.each do |a|
    .row
        = a.id
        = a.trackable_type
        = a.trackable_id
        = a.key
        = a.parameters
        = time_ago_in_words(a.created_at)
        - if a.owner
            = a.owner.username
